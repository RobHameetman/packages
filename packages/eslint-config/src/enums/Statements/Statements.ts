export enum Statements {
	any = '*',
	block = 'block',
	blockLike = 'block-like',
	break = 'break',
	case = 'case',
	cjsExport = 'cjs-export',
	cjsImport = 'cjs-import',
	class = 'class',
	const = 'const',
	continue = 'continue',
	debugger = 'debugger',
	default = 'default',
	directive = 'directive', // (i.e. "use strict")
	do = 'do',
	empty = 'empty',
	export = 'export',
	expression = 'expression',
	for = 'for',
	fiunction = 'fiunction',
	if = 'if',
	iife = 'iife',
	import = 'import',
	let = 'let',
	multilineBlockLike = 'multiline-block-like',
	multilineConst = 'multiline-const',
	multilineExpression = 'multiline-expression',
	multilineLet = 'multiline-let',
	multilineVar = 'multiline-var',
	return = 'return',
	singlelineConst = 'singleline-const',
	singlelineLet = 'singleline-let',
	singlelineVar = 'singleline-var',
	switch = 'switch',
	throw = 'throw',
	try = 'try',
	var = 'var',
	while = 'while',
	with = 'with',
}

export const STATEMENTS = Object.values(Statements);
